const notes= ['note 1', 'note 2', 'note 3']

/* console.log(notes.length) // to print the length of the array
console.log(notes[notes.length - 2]) // to print the second last item in the array
console.log(notes[0]) // to print the first item in the array.
*/

notes.push('new note')
console.log(notes)
console.log (notes.pop())

